# UI: features

city-map.tsx — схематичная доступная SVG-карта. city-simulator.tsx — выбор, отчёт, HTTP-клиент, сохранение, советник и управление ожиданием.

Границы и проверки — в [README UI](../README.md).
