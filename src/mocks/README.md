# Демонстрационные данные

Актуальный прогресс — в [plans.md](../../plans.md), зависимости — в [карте архитектуры](../../docs/architecture/README.md).

## Ответственность

Резерв для будущих демонстрационных fixtures; в текущем runtime не используется.

## Внутренняя структура

Только README. Fake OpenAI находится в tests/integration, отключение AI e2e — в playwright.config.ts.

## Публичный интерфейс

Runtime-экспортов нет; реальные отчёты всегда вычисляет engine.

## Зависимости

Нет исполняемых зависимостей.

## Проверки при реализации

Не подменять произвольный сценарий контрольным Score. Fallback явно маркирован и не считается AI-ответом.

При изменении публичного интерфейса обновляйте contracts, потребителей и этот README. Команды проверки: npm test, npm run typecheck, npm run lint; UI дополнительно npm run test:e2e.
