# План проекта «Аким на 5 часов»

## Текущее состояние

Обновлено: 2026-09-23. Перед работой агент читает этот раздел, затронутый этап и сверяет их с файлами по [AGENTS.md](AGENTS.md). После работы обновляет доказательства и следующий шаг.

| Поле | Фактическое состояние |
| --- | --- |
| Цель | Игровой AI-симулятор городского управления на синтетическом датасете |
| Согласовано | Бюджет 100, пять уникальных мер, максимум две на направление, точные правила Score, отдельные модули и локальный запуск |
| Стек | Next.js 16.3.6, React 19.3.0, TypeScript, Zod, OpenAI SDK / Responses API / gpt-6-astra medium, SQLite |
| Архитектура | Одно приложение с отдельными UI, backend, engine, AI, storage, data, contracts и config |
| Запуск | Сайт/API на 127.0.0.1; AI работает на серверах OpenAI и выполняет веб-поиск |
| Хранение | Локальная SQLite для сценариев и AI-задач; localStorage для черновика; sessionStorage для текущего анализа |
| Текущий этап | Усиление MVP и понятности AI-объяснений (v3) реализовано и проверено; ожидается просмотр владельцем |
| Реализовано | Карта пяти районов, каталог 14 мер, ограничения бюджета, отчёт, AI по запросу/финал с серверными основаниями, источники, отмена/ошибки, сохранение и открытие сценариев |
| Проверено | 68 Vitest, 6 Playwright, lint/typecheck/build/check:data; четыре вида живых AI-проверок и ручной просмотр; подробности в отчёте повторной приёмки |
| Ещё не выполнено | Завершение приёмки владельцем, экспертная оценка всей серии AI-ответов, эмпирическая калибровка под реальный город, сравнение команд/события/презентация |
| Следующий шаг | Просмотр владельцем результатов повторной приёмки; публикация только после его решения. Далее расширение AI-evals и проверенные альтернативы. Сервер/дизайн — отдельно |
| Git | База текущей копии 64f0f10, ветка codex/local-mvp. Усиление после аудита оставлено без commit/push по запросу владельца |
| Ограничения | Один компьютер, синтетическая модель, платный AI и поиск; лимит шесть анализов/минуту не является денежным лимитом |

Путь текущей рабочей копии на компьютере владельца: `/Users/aldos/.codex/worktrees/local-project-plan/test`. Исходная папка `/Users/aldos/Documents/ChatGPT/test` имеет другую Git-историю и не перезаписывалась. Запускать команды из рабочей копии с package.json.

## 1. Продукт и объём

Игрок выбирает пять инициатив и видит распределение бюджета, изменения районов, итоговый Astana Quality of Life Score и компромиссы. Все пять направлений доступны; выбирать по одной мере в каждом не требуется по прямому решению владельца.

Первоначальный порядок «mock-фронтенд → просмотр → backend» изменён последующим запросом: реализовать сразу весь локальный MVP с Astra medium и живым веб-поиском. Просмотр владельцем остаётся следующим этапом обратной связи, но больше не блокирует реализацию backend.

Это учебная модель, не официальный индекс и не прогноз ВВП/здоровья. Движок считает, AI объясняет доверенные числа и исследует реальный контекст. Проверка JSON и идентификаторов не доказывает истинность свободного текста. Нужна экспертная оценка для реальных управленческих решений.

## 2. Репозиторий

Документация P0 подготовлена в codex/local-project-plan от origin/main ca364b3. Прежний agents.md сохранён в [архиве ролей](docs/team-roles.md). По прямому запросу владельца документация опубликована обычным fast-forward push в main; локально подтверждённая вершина — 7e8ae50. Исходная локальная история 65642ab сохранена отдельно.

Реализация MVP подготовлена в codex/local-mvp от 7e8ae50 и опубликована в main обычным fast-forward push (83c91e7). Не объединять несвязанные истории и не делать force-push. Публикация исходников не запускает сайт; приложение остаётся локальным.

## 3. Архитектура и запуск

[README](README.md) содержит воспроизводимые команды. Node.js 22.20+, npm 11.20.0, один package-lock.json. Next.js App Router + React + TypeScript strict; Tailwind, Button на Radix/shadcn-паттерне, Lucide, Recharts. Route Handlers делегируют backend; Zod проверяет вход. Серверные модули защищены server-only, ESLint ограничивает импорты UI/engine.

[Карта архитектуры](docs/architecture/README.md) описывает модули и API. UI использует чистый engine для мгновенного preview, сервер повторно проверяет выбор. SQLite — var/akim.sqlite. OpenAI SDK и ключ остаются на сервере. Облачный хостинг, Supabase и Docker не требуются.

Без ключа или интернета симуляция, серверный расчёт и SQLite работают; AI показывает обозначенный fallback. Для отключения платных вызовов при настроенном ключе: AI_ENABLED=false. Модель не меняется автоматически.

## 4. Данные и точные правила

Источник — предоставленный DOCX. Его полное текстовое содержание и шесть таблиц сохранены в [docs/data/dataset-source.md](docs/data/dataset-source.md); там же SHA-256 оригинала. Числа в этой копии не изменены. Программно проверены суммы весов, долей населения, оценки пяти районов и контрольный пример.

- Районы: Есиль, Алматы, Сарыарка, Байконур, Нура; доли населения 0.27, 0.24, 0.20, 0.13, 0.16.
- Показатели: T1/T2 — транспорт, E1/E2 — экология, S1/S2 — социальная инфраструктура, B1/B2 — безопасность, C1/C2 — городской сервис. Значения 0–100, больше всегда лучше.
- Ровно 5 уникальных мер; бюджет 100; не более 2 мер одного направления. Остаток бюджета не сгорает и не даёт бонуса.
- У районной меры выбран один допустимый район; у городской район не указан, эффект применяется ко всем пяти районам.
- M1 + M3 запрещены при любых районах; M4 + M7 и M5 + M13 запрещены при совпадении района.
- Горизонт H = 8 кварталов. Эффект меры умножается на `(8 − L) / 8`. Это итоговая модель на горизонте, не полноценная поквартальная динамика.
- Синергии: M1 + M2 → T1 +2 в районе M1; M10 + M12 → B1 +2 в районе M10; M5 + M6 → E2 +2 в районе M5. Бонус применяется один раз и не масштабируется лагом.
- Все эффекты и синергии суммируются, затем каждый показатель обрезается до 0–100. Порядок выбора не влияет на результат. Отрицательный эффект M11 на T1 сохраняется.
- Вес показателей в порядке T1, T2, E1, E2, S1, S2, B1, B2, C1, C2: 0.10, 0.10, 0.09, 0.11, 0.11, 0.11, 0.09, 0.09, 0.10, 0.10.

```text
I'[d,k] = clip(I[d,k] + Σ(effect[m,k] × (8 − L[m]) / 8) + synergy[d,k], 0, 100)
D[d]    = Σ(weight[k] × I'[d,k])
D_avg   = Σ(populationShare[d] × D[d])
N_crit  = количество пар «район × показатель», где I'[d,k] < 40
Score   = 0.7 × D_avg + 0.3 × min(D[d]) − N_crit
```

Score не приравнивается к ВВП и не обрезается повторно до 0–100. Округление — только при отображении. Невалидный набор возвращает причины без Score и без обращения к LLM. Исходный город показывается отдельно от валидации отправленного сценария.

### Контрольные значения

| Сценарий | Расход | D_avg | Слабейший D | N_crit | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| Исходный город | 0 | 56.8624 | 49.18 | 2 | 52.55768 |
| M7 Нура, M8 Нура, M10 Нура, M12 город, M5 Сарыарка | 95 | 58.0776 | 52.9625 | 0 | 56.54307 |

Прирост примера — 3.98539, отображение `+3,99`; Score `56,54`. Отдельные округлённые числа могут давать другую разность — delta считается из точных значений. Самый дешёвый набор из исходника стоит 61: M9, M11, M10, M12, M4 с допустимыми назначениями районов.

## 5. Игровой интерфейс

Реализован светлый пульт управления: бюджет/счётчик мер, SVG-карта, десять показателей выбранного района, каталог по пяти направлениям, портфель и панель AI. Карта явно обозначена как схема, реальные границы не заявляются.

Выбор, удаление и смена района меры проверяют бюджет, уникальность, максимум двух мер на направление и конфликты. Полные эффекты показаны с лагом. Неполный набор меняет показатели, но не получает итоговый Score. Отчёт показывает Score/delta, районные графики до/после, таблицы всех показателей, критичность и синергии. Числа реальные для выбранного набора, mock-отчёты не используются.

Черновик восстанавливается из localStorage после проверки версии и правил. Результаты сохраняются в SQLite по кнопке. AI-панель поддерживает вопрос, источники, статусы, отмену и возврат фокуса. Готовый ответ старого портфеля не заменяет новый. Проверены 390/1440 px и основные действия клавиатурой; полноценный аудит доступности пока не выполнен.

## 6. Этапы и критерии

| Этап / статус | Зависимости | Реализуемый результат | Критерии и доказательства |
| --- | --- | --- | --- |
| P0 — готов | Кейс и DOCX | План, инструкции, архитектура и источники | Таблицы/контрольные числа, README/ссылки, публикация документации |
| P1 — готов | P0 | Работающий локальный игровой UI | Выбор/ограничения/отчёт, 390/1440 px, клавиатура, Playwright; по новому запросу используется настоящий движок |
| P2 — в работе | P1 | Обратная связь владельца | Владелец проходит сценарий; замечания записаны и принятые изменения проверены |
| P3 — готов | Датасет | Чистый движок | Vitest всех числовых входов, контрольных значений и правил |
| P4 — готов | P3 | Backend и HTTP-контракты | Серверный пересчёт, отклонение подмены Score/цены, сквозной сценарий |
| P5 — готов в объёме MVP | P4 | Astra medium, веб-поиск и объяснение | Реальные чат/финал, источники/схема, fake-provider тесты ошибок/отмены/дедлайна; смысловая оценка ограничена небольшой серией, включая один новый финальный ответ |
| P6 — готов в объёме MVP | P4/P5 | SQLite, сценарии и AI-задачи | Чистая миграция, сохранение/чтение и открытие после закрытия БД; тесты жизненного цикла |
| P7 — готов на текущем macOS | P1/P3–P6 | Воспроизводимый локальный запуск | Чистая копия без ключа/БД: npm ci, build, start, HTTP, Score, SQLite и fallback |

Код запускается без прохождения P2 по последнему прямому запросу владельца. «Готов» относится к указанному объёму и проверенному окружению; Windows/Linux, публичный хостинг, нагрузка и экспертная валидация не проверены.

## 7. Контракты и доверенные данные

Selection содержит measureId и districtId (null для города). ScenarioInput: версии и selections. POST /api/simulate принимает также mode preview/final. Лишние поля отклоняются строгой схемой; цены/эффекты/Score рассчитываются сервером. Невалидный набор получает HTTP 400 и причины, без Score и без LLM.

ScenarioReport содержит базу, итоговые показатели, стоимость/остаток, Score/delta, критичность, вклады мер после лага, синергии, clipping и factIds. Для неполного preview score/delta=null. Вклад в показатели не следует представлять как аддитивный вклад в нелинейный Score.

POST /api/analyses получает тот же выбор, kind step/chat/final, вопрос, контекст и UUID requestId/sessionId. Сервер пересчитывает отчёт; для previousSelections добавляет проверенные факты с префиксом previous. GET статуса и DELETE отмены работают по локальному ID. POST/GET /api/scenarios сохраняют/открывают серверный отчёт. Точные схемы — в [contracts](src/contracts/README.md).

SQLite хранит сценарии и задания; AI-задания содержат модель, reasoning, promptVersion, usage, источники и отчёт с версиями. Дедупликация по requestId не является семантическим кэшем одинаковых вопросов: такой кэш пока не реализован. SQL скрыт в storage; ошибка сохранения не отменяет уже показанный расчёт.

## 8. AI и интернет

Изменение портфеля пересчитывает preview и отменяет старое ожидание, но не запускает новый платный анализ. Вопрос советнику и кнопка итогового разбора запускают chat/final. Простое открытие карточки, переключение карты, reset и загрузка примера запрос не создают.

Один анализ — два background response: исследование с обязательным web_search, затем Structured Outputs с реестром цитат. Модель gpt-6-astra, effort medium, максимум 5000/7000 output tokens соответственно, retries=0. Промпт urban-advisor-3 требует учитывать контекст Астаны и различать игровые числа, исследования и гипотезы. Нельзя выводить ВВП или зарплаты из обеспеченности школами: таких переменных нет.

JSON и factIds/sourceIds проверяются. Источники context обязательны; URL берутся из url_citation, а не из произвольного текста. Эти проверки не гарантируют семантическую корректность; анализ экспертом и расширенная серия evals остаются дальнейшей работой. [AI-спецификация](docs/superpowers/specs/2026-09-23-openai-astra-integration-design.md) описывает промпт и ограничения.

Лимиты: шесть стартов в минуту на сессию, до 3000 символов вопроса, 180 секунд ожидания. Браузер опрашивает статус; без опроса переход к следующему этапу не происходит. По дедлайну новая платная фаза не начинается; уже завершённый финальный ответ возвращается и при позднем опросе. Отмена и прерванный запуск не вызывают автоматического платного повтора. БД хранит состояние, UI восстанавливает ID задачи после перезагрузки вкладки.

## 9. Мировые практики

[Справочник источников](docs/research/README.md) сохранён. Runtime выполняет поиск по вопросу/мерам, а не подменяет его справочным текстом. Промпт предпочитает исследования и официальные материалы, проверяет контекст климата, сезонности, обслуживания и экономики. Неизвестные местные данные отмечаются ограничением. Публикация программы не доказывает причинный эффект.

Исследования не меняют коэффициенты датасета. Для реалистичного градостроительного режима нужны местные данные, калибровка, причинная модель, интервалы неопределённости и более длинный горизонт. Это отдельный будущий этап.

## 10. Ограничения и следующий объём

Многопользовательские аккаунты, рейтинг команд, случайные события, презентации и автоматический поиск улучшений не реализованы. Пока нет фонового worker вне опросов браузера, денежного лимита API или полностью автоматической проверки истинности текста. Все пользователи начинают одинаково; события в базовом режиме отсутствуют.

Следующий запрос по продукту: показать владельцу локальное приложение, пройти контрольный и собственный портфели, собрать замечания к читаемости, механике и качеству объяснений. Затем приоритизировать конкретные изменения и повторить затронутые проверки. Не считать просмотр владельцем выполненным на основании скриншота агента.

## 11. Журнал решений и проверок

### Решения

| Дата | Решение | Основание |
| --- | --- | --- |
| 2026-09-23 | Пять мер, максимум две на направление | Явный выбор владельца по датасету |
| 2026-09-23 | Полностью локальный сайт/backend, SQLite, внешний OpenAI API | Запрос владельца; облачный деплой исключён |
| 2026-09-23 | Одно приложение с изолированными модулями и README | Требование понятной архитектуры |
| 2026-09-23 | Отдельная ветка от origin/main; затем публикация документации в main | Сохранение разных историй и последующий прямой запрос владельца |
| 2026-09-23 | Вместо остановки на mock-UI реализовать весь MVP | Последний запрос владельца: frontend, backend, AI и полноценный тест |
| 2026-09-23 | Astra medium + обязательный живой поиск на анализ | Запрос владельца; модель доступна ключу при проверке |
| 2026-09-23 | Два background этапа с сохранением состояния | Поиск и структурированный вывод разделены; обеспечены восстановление и отмена |
| 2026-09-23 | Отметить завершённые этапы и опубликовать текущий MVP вместе с планом в main | Последний прямой запрос владельца |
| 2026-09-23 | При позднем poll получать готовый ответ до проверки дедлайна | Обнаружен ложный таймаут при паузе браузера; исправлено и проверено |

### Проверки

Все строки относятся к 2026-09-23. Автотесты OpenAI не вызывают. Платные проверки выполнены отдельно, два анализа по два response. Ошибки и исправления перечислены, чтобы не скрывать границы проверки.

| Дата | Команда / действие | Результат | Ограничения |
| --- | --- | --- | --- |
| 2026-09-23 | npm test | PASS: 34 теста, 4 файла | Fake provider вместо сети; не экспертная оценка текста |
| 2026-09-23 | npm run typecheck; npm run lint | PASS | Типы и настроенные правила, не полный архитектурный аудит |
| 2026-09-23 | npm run test:e2e | PASS: 4 сценария Chromium | AI_ENABLED=false; desktop и 390 px, не все браузеры |
| 2026-09-23 | npx --yes npm@11.20.0 ci | PASS в чистой копии без .env/БД, 0 известных уязвимостей audit | Проверено на текущем macOS/Node 22.20; npm 10 install ранее дал внутреннюю ошибку резолвера |
| 2026-09-23 | npm run build | PASS в чистой копии, все маршруты | Сборка без ключа; не доказывает доступ к API |
| 2026-09-23 | npm start -- --port 3001; HTTP smoke через Node fetch | PASS: HTTP 200, Score 56.54307, SQLite save/read, fallback без ключа | Временная чистая копия; отдельный порт |
| 2026-09-23 | GET /v1/models/gpt-6-astra серверным ключом | HTTP 200, модель доступна | Доступ/квота могут измениться |
| 2026-09-23 | Реальный chat: парки/спорт в Астане | completed, 4 источника; usage input 35764 / output 2209 | Два этапа; проверены схема/ID и пример текста, не все утверждения |
| 2026-09-23 | Реальный final контрольного портфеля | completed, 4 источника; usage input 31823 / output 2173; прирост 3,99 объяснён | Первоначальный локальный ложный timeout исправлен; готовый response получен повторным чтением без новой генерации |
| 2026-09-23 | Проверка исходного DOCX/P0, публикация документации | Сохранены числа/источники, обычный push в main до 7e8ae50 | Исторический этап документации; публикация кода проверена отдельной строкой |

| 2026-09-23 | python3 scripts/check_docs.py | PASS: 10 модулей, 26 Markdown, 69 локальных ссылок, регистр AGENTS.md, шаблон/env-ignore и паттерны секретов | Не проверяет доступность внешних URL и отсутствие любых неизвестных форматов секретов |
| 2026-09-23 | python3 scripts/verify_dataset.py | PASS: 6 таблиц, 5 районов, 14 мер, 3 синергии; Score 52.55768 / 56.54307 | Независимая Decimal-проверка источника, дополняет runtime-тесты |
| 2026-09-23 | npm run build в рабочей копии с .env; проверка клиентских файлов | PASS: сборка; значение локального ключа не найдено в 67 клиентских файлах | Проверен именно текущий ключ и текущая сборка; значение не выводилось |
| 2026-09-23 | Playwright: визуальный просмотр и восстановление реального анализа | PASS: 1440/390 px, 56,54, четыре ссылки, Escape/возврат фокуса, 0 ошибок JS | Уже готовый response, без новой генерации; не полный accessibility-аудит |
| 2026-09-23 | git diff --check | PASS | Проверка diff, не поведения приложения |
| 2026-09-23 | git push origin HEAD:refs/heads/main; git ls-remote origin refs/heads/main | PASS: 7e8ae50 → 83c91e7a502cffb3628d195c124555290513f604, удалённый SHA совпал с HEAD | Обычный SSH push полного MVP; эта отметка добавлена после подтверждения |

### Повторная проверка по запросу владельца

2026-09-23, код `64f0f10`: повторно выполнены `npm test` (34/34), `npm run test:e2e` (4/4 Chromium), `npm run typecheck`, `npm run lint`, `npm run check:data`, `npm run check:docs`, `AI_ENABLED=false npm run build` и `git diff --check` — все успешно. Браузерные проверки включают контрольный Score 56,54, сохранение/открытие, fallback, конфликт, черновик, клавиатуру, ширину 390 px и отклонение подмены/чужого Origin. Реальный OpenAI в этом прогоне не вызывался; доступность генерации и качество нового ответа не проверены. Следующий шаг — просмотр приложения владельцем (P2).

### Прогресс

- [x] Согласованы кейс, правила, стек и локальный режим.
- [x] P0: документация, данные и источники, публикация документации.
- [x] P1: игровой фронтенд, карта, каталог инициатив и отчёт.
- [x] P3: расчётный движок, правила и тесты исходных чисел.
- [x] P4: backend, серверная валидация и API.
- [x] P5: GPT-6 Astra medium, веб-поиск, реальные AI-ответы и обработка ошибок.
- [x] P6: SQLite, сохранение/открытие сценариев и состояния анализа.
- [x] P7: чистая установка, сборка, production-запуск и сквозные проверки.
- [ ] **P2 — в работе: аудит завершён, локальное усиление реализовано; ожидается просмотр владельцем.**
- [x] Публикация кода MVP и актуального плана в GitHub main.

## 12. Основание инструкций для GPT-6 Astra

Применены официальные рекомендации: короткие инструкции с особенностями проекта; подробности загружаются по задаче; понятная граница этапа; соразмерная проверка; явное различение факта и предположения. AGENTS.md не гарантирует отсутствие ошибок модели — достоверность поддерживают исходные данные, серверные ограничения, контрольные примеры и проверка результатов.

- [OpenAI: Custom instructions with AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md) — имя файла, область действия и обнаружение инструкций.
- [OpenAI: Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra) — компактный контекст, релевантное чтение, ясные границы завершения.
- [OpenAI: GPT-6 prompting best practices](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices) — инициатива, следование инструкциям, стиль и объём проверок.

Источники проверены 2026-09-23; рекомендации по модели прочитаны на официальной странице Using GPT-6.

- [OpenAI: Run long horizon tasks with Codex](https://developers.openai.com/blog/run-long-horizon-tasks-with-codex) — этапы, критерии приёмки, проверки, актуальные статусы и решения. Прочитано 2026-09-23.

### Аудит соответствия кейсу — 2026-09-23

Владелец просмотрел интерфейс и сообщил, что он в целом устраивает. Подготовлен [аудит требований и реализации](docs/reviews/case-audit-2026-09-23.md). Полного брифа организаторов в доступном дереве/списке файлов истории не найдено; запрошена ссылка. Локальный эксперимент подтвердил, что AI-валидатор принимает неверное число с пустым или существующим factId; это не реальный ответ модели. Выявлены пробелы объяснимости отчёта, риск исчерпания минутного лимита перед финальным анализом и отсутствие независимого доступа судьи. Исполняемый код не менялся; новые платные вызовы не выполнялись. Следующий шаг — сверка официального кейса и выбор конкретных исправлений.

В ходе аудита владелец предоставил полный текст кейса и критерии на 100 баллов: содержание сохранено в [docs/case.md](docs/case.md), ссылки добавлены в README. Аудит обновлён: шесть must-have представлены в коде; доказательства качества реальных AI-объяснений ограничены. Сравнение команд, события и презентации опциональны. Публичный деплой не является формальным must-have; самостоятельный доступ судьи к AI без своего ключа требует отдельного решения.

### Локальное усиление после аудита

По запросу владельца 2026-09-23 реализованы исправления без публикации; дизайн и серверный запуск не входят в этот этап. Подробности, оставшиеся ограничения и способ просмотра — [отчёт усиления](docs/reviews/local-mvp-hardening-2026-09-23.md).

Обновлённый отчёт показывает состав, вклады мер, критические показатели, слабейший район и формулу. AI вызывается явно; количественные основания показывает UI из серверных фактов. Добавлена ограниченная проверка числового текста и обязательность оснований; это не доказательство смысловой достоверности. Финальный разбор сохраняется со сценарием. Исправлены дедупликация во время отмены, поздние ответы и восстановление анализа другого черновика.

Проверено: 53/53 Vitest (5 файлов), 5/5 Playwright Chromium (включая новый отчёт на 390 px), lint, typecheck и production build. Реальный final urban-advisor-2 завершён: 4 источника, usage 30606/2493; сохранён в локальных сценариях. Новая чистая установка не выполнялась, зависимости не менялись. Локальные изменения не коммитились и не отправлялись в GitHub.

### Понятные объяснения — urban-advisor-3

По запросу владельца улучшены промпт, шаблонный ответ и последовательность подачи AI-объяснения. Основной вывод, меры/районы, риски и действия доступны сразу; мировой опыт раскрывается отдельно, технические подписи фактов расшифрованы. Защита фактов/чисел, модель gpt-6-astra medium и математика сохранены.

Проверки: 57/57 Vitest (6 файлов), 6/6 Playwright, lint/typecheck/build — PASS. Один реальный final v3 completed, 4 источника, usage 43972/3104. В контрольном примере главный вывод сократился с 31 до 19 слов, весь текст с 260 до 225; это ограниченная оценка, не гарантия качества. Готовый ответ сохранён в «Понятный AI-разбор — версия 3» и открыт в браузере без новой генерации.

[Отчёт с примерами до/после](docs/reviews/ai-readability-2026-09-23.md). Следующий шаг — просмотр владельцем и серия оценок понятности на разных final/chat. Все изменения остаются локальными без commit/push.

### Повторная приёмка AI — 2026-09-23

По запросу владельца выполнены 68/68 Vitest, 6/6 Playwright, lint/typecheck/build/check:data и ручные сценарии в браузере. Исправлены ложные срабатывания проверки числительных и сохранение архивного объяснения после смены промпта; исходный профиль района явно подписан. Сделано семь реальных анализов: три первоначальных ложных отказа устранены, четыре вида случаев представлены успешными ответами. Проверка провокации не выявила вымышленных ВВП/зарплат/здоровья; это ограниченная выборка. [Результаты и полные ответы](docs/reviews/ai-acceptance-2026-09-23.md). Публикация не выполнялась; следующий шаг — просмотр владельцем.
